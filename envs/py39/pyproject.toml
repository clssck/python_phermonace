[project]
name = "py39-benchmarks"
version = "0.1.0"
description = "Python 3.9 benchmark environment"
requires-python = ">=3.9,<3.10"
dependencies = [
    "numpy>=1.24.0,<2.0.0",
    "pandas>=2.0.0",
    "rich>=13.0.0",
]

[tool.uv]
python-preference = "only-managed"
