[project]
name = "py313-benchmarks"
version = "0.1.0"
description = "Python 3.13 benchmark environment"
requires-python = ">=3.13,<3.14"
dependencies = [
    "numpy",
    "pandas",
    "rich",
    "orjson",
]

[tool.uv]
python-preference = "only-managed"
