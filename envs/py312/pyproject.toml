[project]
name = "py312-benchmarks"
version = "0.1.0"
description = "Python 3.12 benchmark environment"
requires-python = ">=3.12,<3.13"
dependencies = [
    "numpy",
    "pandas",
    "rich",
    "orjson",
]

[tool.uv]
python-preference = "only-managed"
