[project]
name = "py314-benchmarks"
version = "0.1.0"
description = "Python 3.14 benchmark environment"
requires-python = ">=3.14"
dependencies = [
    "numpy>=1.24.0",
    "pandas>=2.0.0",
    "rich>=13.0.0",
]

[tool.uv]
python-preference = "only-managed"
