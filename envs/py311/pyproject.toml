[project]
name = "py311-benchmarks"
version = "0.1.0"
description = "Python 3.11 benchmark environment"
requires-python = ">=3.11,<3.12"
dependencies = [
    "numpy",
    "pandas",
    "rich",
    "orjson",
]

[tool.uv]
python-preference = "only-managed"
